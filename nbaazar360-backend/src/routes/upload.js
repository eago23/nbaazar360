const express = require('express');
const router = express.Router();

// All upload routes are admin-only and handled in /api/admin/upload/*
// This file is kept for potential future public upload routes

module.exports = router;
